1. What is the difference between getElementById, getElementsByClassName, and querySelector / querySelectorAll?
Ans: Selects an element by its id.
     Selects elements by their class name.
     Selects the first element that matches a CSS selector.
     Selects all elements that match a CSS selector.

2.  How do you create and insert a new element into the DOM?
    createElement()
    Add content/attributes
    Use append(), appendChild(), prepend()     

3. What is Event Bubbling? And how does it work?
    Event Bubbling is a process where an event starts from the target element and propagates upward through its ancestor elements in the DOM.

4. What is Event Delegation in JavaScript? Why is it useful?
    Event Delegation is a technique where a single event listener is attached to a parent element to handle events for multiple child elements using event bubbling.

5. What is the difference between preventDefault() and stopPropagation() methods?
    preventDefault() stops the browser’s default action for an event, while stopPropagation() prevents the event from bubbling up the DOM tree.    